#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0 (success)
 */

int main(void)
{
	char p;

	char q;

	p = 'a';
	q = 'A';
	while
		(p <= 'z') {
			putchar(p);
			p++;
		}
	while
		(q <= 'Z') {
			putchar(q);
			q++;
		}
	putchar('\n');
	return (0);
}
